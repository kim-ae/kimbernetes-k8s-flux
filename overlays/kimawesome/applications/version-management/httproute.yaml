apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: version-management-route
spec:
  parentRefs:
    - name: https-gateway
      namespace: kube-system
      sectionName: https
  hostnames:
    - "kim.tplinkdns.com"       # Specific hostname for this app
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /version-management
      backendRefs:
        - name: version-management
          port: 3000