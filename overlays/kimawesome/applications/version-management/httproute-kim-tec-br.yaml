apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: version-management-route-kim-tec-br
spec:
  parentRefs:
    - name: kim-tec-br-gateway
      namespace: kube-system
      sectionName: https
  hostnames:
    - "version-management.kim.tec.br"       # Specific hostname for this app
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /version-management
      backendRefs:
        - name: version-management
          port: 3000