# PLACEHOLDER — DO NOT COMMIT AS-IS.
# Generate the real file with:
#   kubeseal --format=yaml \
#     --cert=<path-to-sealed-secrets-pub-cert> \
#     < .ignore-pve-exporter-secret.yaml > pve-exporter-secrets.sealed.yaml
# then replace this file's contents with the kubeseal output before applying/committing.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: pve-exporter-secrets
spec:
  encryptedData: {}
  template:
    metadata:
      name: pve-exporter-secrets
