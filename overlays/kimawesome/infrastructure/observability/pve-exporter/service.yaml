apiVersion: v1
kind: Service
metadata:
  name: pve-exporter
  labels:
    app: pve-exporter
spec:
  type: ClusterIP
  selector:
    app: pve-exporter
  ports:
    - name: metrics
      port: 9221
      targetPort: 9221
      protocol: TCP
