server {
    listen 8080;
    server_name knowledge-hub.kim.tec.br leituras.kim.tec.br;
    
    root /usr/share/nginx/html;
    index index.html;
    
    # Serve the site at root path for these domains
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Serve CSS files
    location ~ \.css$ {
        add_header Content-Type text/css;
        expires 1d;
        add_header Cache-Control "public, immutable";
    }
    
    # Serve JS files
    location ~ \.js$ {
        add_header Content-Type application/javascript;
        expires 1d;
        add_header Cache-Control "public, immutable";
    }
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}

server {
    listen 8080;
    server_name kim.tec.br;
    
    root /usr/share/nginx/html;
    index index.html;
    
    # Serve the site at /knowledge-hub path for this domain
    location /knowledge-hub {
        alias /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
    
    # Handle CSS files for knowledge-hub path
    location /knowledge-hub/styles.css {
        alias /usr/share/nginx/html/styles.css;
        add_header Content-Type text/css;
        expires 1d;
        add_header Cache-Control "public, immutable";
    }
    
    # Handle JS files for knowledge-hub path
    location /knowledge-hub/script.js {
        alias /usr/share/nginx/html/script.js;
        add_header Content-Type application/javascript;
        expires 1d;
        add_header Cache-Control "public, immutable";
    }
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}